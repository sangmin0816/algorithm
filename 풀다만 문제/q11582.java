package 풀다만문제;
import java.util.*;
/*
public class q11582 { // https://www.acmicpc.net/problem/11582
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        long N = sc.nextLong();
        ArrayList<Integer> ctp = new ArrayList<Integer>();
        for(long i=0; i<N; i++) ctp.add(sc.nextInt());
        long K = N/sc.nextLong();

        while(ctp.isEmpty()==false){
            ArrayList<Integer> temp = new ArrayList<Integer>();
            for(long i=0; i<K; i++) temp.add(ctp.remove(0));
            Collections.sort(temp);
            for(long i=0; i<K; i++) System.out.printf("%d ", temp.remove(0));
        }

        sc.close();
    }
    public static void merge(int list[], int start, int mid, int end){
        int i, j, k, l;
        i=start; j=mid+1; k=end;
        while(i<=mid&&j<=end){
            if(list[i]<=)
        }
    }

    public static void mergeSort(int list[], int start, int end){
        if(start<end){
            int mid=(start+end)/2;
            mergeSort(list, start, mid);
            mergeSort(list, mid+1, end);
            merge(list, start, mid, end);
        }
        
    }
}
*/